#include "main.h"

/**
 * _islowercase - checks for lowercase character
 * @c: the character to check
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islowercase(int c)
{
	return (c >= 'a' && c <= 'z');
}


